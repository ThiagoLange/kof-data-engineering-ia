# Lab 8.4 — Feature Store (kof.db + cache)
## Objetivo
Computar features por cidade, servir online via `FeatureStore` (cache) e materializar offline em `features.jsonl` (simula `kof.db`/`kof.cache`).
## Comandos
```bash
kof run modulo-08-data/04-feature-store/lab.kof
kof test modulo-08-data/04-feature-store/exercise.kof
```
