# Lab 9.2 — Perf Native vs JVM (kof profile)
## Objetivo
Demonstrar `kof profile` (RSS/CPU) e comparar `kof build --target native|js` gates.
## Comandos
```bash
kof run modulo-09-portabilidade/02-perf/lab.kof
kof profile modulo-04-deep/01-tensor/lab.kof
kof build --target native modulo-04-deep/01-tensor/lab.kof
```
